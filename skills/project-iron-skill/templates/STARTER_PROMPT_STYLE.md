# Starter Prompt Style

Use this interaction style in new projects:

- Give concrete, executable constraints.
- Require append-only raw prompt archive.
- Require rule sync checks on each new intake.
- Require unified ops entrypoint: `python ops.py restart`.
- Require date-layered archive of every LLM conversation output.
